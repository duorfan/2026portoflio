node_modules/
.DS_Store

# Local Claude Code permissions — per-machine, never push.
.claude/settings.local.json

# Inspection artifacts (regenerated by `npm run verify` / `npm run shoot`).
diagnostics/

# Raw CDP capture — large binary tree regenerated by `npm run crawl`.
# Vercel doesn't need it. (Existing tracked files are left as-is.)
captured/

# NOTE: `site/` is intentionally NOT ignored. Vercel deploys those static
# files directly, so the built output has to live in the repo.
